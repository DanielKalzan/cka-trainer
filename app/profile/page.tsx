import PageHeader from "@/components/PageHeader";

export default function ProfilePage() {
  return (
    <PageHeader
      title="Profile"
      subtitle="Stats, badges and history — lands with the gamification layer (Phase 4)."
    />
  );
}
